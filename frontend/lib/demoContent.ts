// Must stay in sync with backend/app/demo_content.py's SAMPLE_FILENAME.
// Used to gate demo-only UI (the forward-pass/activation visualizers) so it
// only ever shows up on the neural-network sample document, not real uploads.
export const SAMPLE_FILENAME = "How Neural Networks Learn.txt";
